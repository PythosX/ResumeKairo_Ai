# Vertex

Leadership A4 resume template with a dedicated profile photo box.

## Dynamic integration
Replace elements marked with `data-field` and replace `.photo-box` with an `<img>` using the user's uploaded photo.